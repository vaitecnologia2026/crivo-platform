-- Usuários CRIVO no painel (pedido do cliente 09/07): função organizacional
-- (Comercial/Financeiro/Operações…). Hoje rótulo; permissões por função = fase posterior.
ALTER TABLE "super_admins" ADD COLUMN "role" TEXT;
